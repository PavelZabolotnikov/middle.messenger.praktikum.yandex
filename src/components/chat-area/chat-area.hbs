<div class='chat-area'>
  {{#if chat}}
    <div class='chat-header'>
      <div class='user'>
        {{#if chat.avatar}}
          <img class='user-avatar' src={{chat.avatar}} alt='' />
        {{else}}
          <img class='user-avatar' src='' alt='' />
        {{/if}}
        <p class='user-name'>{{chat.title}}</p>
      </div>
      <div class='buttons'>
        {{{DeleteChat}}}
        {{{AddUserButton}}}
        {{{RemoveUser}}}
      </div>
    </div>
    {{#if messages}}
      <div class='messages' id='messages'>
        {{#each messages}}
          <div class='message'>
            <p class='message__author'>автор: {{user_id}}</p>
            <p class='message__text'>{{content}}</p>
            <p class='message__date'>{{time}}</p>
          </div>
        {{/each}}
      </div>
    {{else}}
      Сообщений еще нет
    {{/if}}
    {{{MessageInput}}}
  {{else}}
    <p class='attention'>Выберите чат чтобы отправить сообщение</p>
  {{/if}}
</div>
{{{ModalNewUser}}}
{{{ModalRemoveUser}}}
